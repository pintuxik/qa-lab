# Backend Unit Test Environment Configuration
# Loaded automatically by pytest-dotenv before tests run

# Testing flag
TESTING=true

# Test mode disabled for unit tests (not needed with in-memory DB)
TEST_MODE_ENABLED=false
