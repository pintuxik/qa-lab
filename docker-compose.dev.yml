# ============================================================================
# DEVELOPMENT CONFIGURATION - DO NOT USE IN PRODUCTION!
# ============================================================================
# This configuration is for LOCAL DEVELOPMENT ONLY with hot reload support.
# Usage: docker-compose -f docker-compose.dev.yml up
#
# ⚠️  SECURITY WARNING:
# - TEST_MODE_ENABLED=true exposes /api/auth/test-cleanup endpoint
# - This allows deletion of users via API key (for test cleanup)
# - NEVER deploy this configuration to production environments!
#
# 📋 TODO: Create production configuration during CI/CD implementation
# - Create docker-compose.prod.yml with TEST_MODE_ENABLED=false
# - Or use environment-specific .env files
# ============================================================================

services:
  db:
    image: postgres:18
    environment:
      POSTGRES_DB: taskmanager
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:password@db:5432/taskmanager
      - SECRET_KEY=your-super-secret-key-change-in-production
      - FRONTEND_URL=http://localhost:5001
      # ⚠️ TEST MODE - Enables test cleanup endpoint (DEVELOPMENT ONLY!)
      - TEST_MODE_ENABLED=true
      - TEST_API_KEY=your-super-secret-key-change-in-production
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - backend_venv:/app/.venv  # Keep Docker venv separate from host
    command: bash start.sh

  frontend:
    build: ./frontend
    ports:
      - "5001:5001"
    environment:
      - API_BASE_URL=http://backend:8000
      - SECRET_KEY=your-super-secret-key-change-in-production
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - frontend_venv:/app/.venv  # Keep Docker venv separate from host
    command: bash start.sh

volumes:
  postgres_data:
  backend_venv:    # Named volume for backend venv
  frontend_venv:   # Named volume for frontend venv
