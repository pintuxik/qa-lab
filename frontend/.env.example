# Frontend Environment Variables Configuration
# Copy this file to .env and update the values for your environment

# ============================================================================
# BACKEND API CONFIGURATION
# ============================================================================
# Backend API base URL
# This should point to your FastAPI backend service
# Development (Docker): http://backend:8000
# Development (Local): http://localhost:8000
# Production: https://api.your-domain.com
API_BASE_URL=http://backend:8000

# ============================================================================
# FLASK CONFIGURATION
# ============================================================================
# Secret key for Flask session management and CSRF protection
# IMPORTANT: Change this to a strong random string in production
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-change-in-production

# Flask environment mode
# Development: development (enables debug mode, auto-reload)
# Production: production (disables debug features)
# FLASK_ENV=production

# Flask debug mode (DO NOT enable in production)
# Development: True
# Production: False
# FLASK_DEBUG=False

# ============================================================================
# SESSION CONFIGURATION (Optional)
# ============================================================================
# Session cookie settings (optional, Flask defaults are reasonable)
# SESSION_COOKIE_SECURE=True       # Enable in production with HTTPS
# SESSION_COOKIE_HTTPONLY=True     # Recommended for security
# SESSION_COOKIE_SAMESITE=Lax      # CSRF protection

# ============================================================================
# LOGGING CONFIGURATION (Optional)
# ============================================================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# ============================================================================
# NOTES FOR DEPLOYMENT
# ============================================================================
# Production Checklist:
# [ ] Generate a strong SECRET_KEY (min 32 characters)
# [ ] Update API_BASE_URL to production backend URL
# [ ] Set FLASK_ENV=production
# [ ] Set FLASK_DEBUG=False
# [ ] Enable SESSION_COOKIE_SECURE with HTTPS
# [ ] Use environment-specific settings (staging, production)
# [ ] Never commit .env file to version control
# [ ] Ensure backend API is accessible from frontend
# [ ] Configure proper CORS settings on backend
# [ ] Use a reverse proxy (nginx, Caddy) in production
# [ ] Enable rate limiting for API requests
