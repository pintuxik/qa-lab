# ============================================================================
# DOCKER COMPOSE LOCAL DEVELOPMENT CONFIGURATION
# ============================================================================
# This file is a template for docker-compose local development and testing.
# Variable names are unified with Kubernetes deployment for consistency.
#
# Setup:
#   Option 1: Run ./setup-env.sh (recommended)
#   Option 2: Copy this file to .env manually: cp .env.example .env
#
# Usage: docker-compose up --build
#
# WARNING: Do not use these values in production!
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION (matches K8s pattern)
# ============================================================================
# Non-sensitive connection parameters
DB_HOST=db
DB_PORT=5432
DB_NAME=taskmanager

# Sensitive credentials (in K8s these come from Secrets)
DB_USER=postgres
DB_PASSWORD=password

# ============================================================================
# APPLICATION SECRETS
# ============================================================================
# JWT signing key for backend
SECRET_KEY=local-dev-secret-key-do-not-use-in-production

# Flask session key for frontend
FLASK_SECRET_KEY=local-dev-flask-secret-do-not-use-in-production

# ============================================================================
# SERVICE CONFIGURATION
# ============================================================================
# Backend settings
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
FRONTEND_URL=http://localhost:5001

# Frontend settings
API_BASE_URL=http://backend:8000

# ============================================================================
# TEST MODE CONFIGURATION
# ============================================================================
# Enable test cleanup endpoint (for integration tests)
# WARNING: Never enable in production!
TEST_MODE_ENABLED=true
TEST_API_KEY=local-test-api-key-do-not-use-in-production
