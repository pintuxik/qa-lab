# Root Environment Variables Configuration (Integration Tests)
# Copy this file to .env and update the values for your environment

# ============================================================================
# INTEGRATION TEST CONFIGURATION
# ============================================================================
# API base URL for integration tests
# This should point to the running backend service
# Local: http://localhost:8000
# Docker: http://backend:8000
API_BASE_URL=http://localhost:8000

# Frontend URL for UI tests
# This should point to the running frontend service
# Local: http://localhost:5001
# Docker: http://frontend:5001
FRONTEND_URL=http://localhost:5001

# ============================================================================
# PLAYWRIGHT CONFIGURATION
# ============================================================================
# Run Playwright tests in headless mode
# Set to "false" to see browser during test execution (useful for debugging)
# Set to "true" for CI/CD pipelines
HEADLESS=true

# Browser slow motion in milliseconds (for debugging)
# Set to 0 for normal speed, increase for easier visual debugging
# SLOW_MO=300

# ============================================================================
# TEST EXECUTION CONFIGURATION
# ============================================================================
# Number of parallel workers for pytest-xdist
# "auto" uses all available CPU cores
# Or specify a number: "4", "8", etc.
# PYTEST_WORKERS=auto

# Test timeout in seconds
# API_TIMEOUT=10
# UI_TIMEOUT=30

# ============================================================================
# ALLURE REPORTING (Optional)
# ============================================================================
# Directory for Allure test results
# ALLURE_RESULTS_DIR=allure-results

# ============================================================================
# DATABASE CONFIGURATION (For integration tests)
# ============================================================================
# These are typically set by docker-compose but can be overridden
# POSTGRES_DB=taskmanager
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=password
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# ============================================================================
# TEST CLEANUP CONFIGURATION
# ============================================================================
# API key for /api/auth/test-cleanup endpoint from backend
# Make sure you set TEST_MODE_ENABLED=true in backend .env
TEST_API_KEY=your-super-secret-key-change-in-production

# ============================================================================
# NOTES FOR CI/CD
# ============================================================================
# CI/CD Environment Checklist:
# [ ] Set HEADLESS=true for automated test runs
# [ ] Configure API_BASE_URL to match your CI environment
# [ ] Set FRONTEND_URL to match your CI environment
# [ ] Ensure all services are running before tests
# [ ] Use docker-compose for consistent test environments
# [ ] Install Playwright browsers: playwright install --with-deps
# [ ] Generate Allure reports after test execution
# [ ] Archive test artifacts (screenshots, traces, videos)
# [ ] Set appropriate timeouts for your infrastructure
# [ ] Check that TEST_MODE_ENABLED=true in backend .env
# [ ] Copy/paste TEST_API_KEY from backend configuration
